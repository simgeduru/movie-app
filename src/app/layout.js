import { Lato, Sedan } from "next/font/google";
import "./globals.css";
import Header from "../components/Header";


const lato = Lato({subsets:["latin"], weight:['100', '300', '400', '700', '900']});
//const sedan = Sedan({subsets:["latin", "latin-ext"], weight:'400', variable:'--font-sedan', display:'swap'})

export const metadata = {
  title: "Movie App",
  description: "Generated by Simge DURU",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" className="h-full">
      <body className= {"font-sedan  bg-[#001524] p-10 h-full"}>
        <div className="p-5 h-full container">
        <Header></Header>
        <div className="h-2/5 mt-10"> image is here</div>
        <div className="text-center">top rated films here</div>
        {children}
        </div>
        
        </body>
    </html>
  );
}
